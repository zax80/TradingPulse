services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: tradingpulse
      POSTGRES_USER: tradingpulse
      POSTGRES_PASSWORD: tradingpulse
    ports:
      - "5432:5432"
    volumes:
      - tradingpulse-data:/var/lib/postgresql/data

volumes:
  tradingpulse-data: